import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#1565c0" // насыщенный синий
    },
    secondary: {
      main: "#546e7a" // сине-серый
    },
    background: {
      default: "#f4f6f8"
    }
  }
});

